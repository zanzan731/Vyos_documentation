auths:
  vyos_auth:
    version: 2
    community: startup11

modules:
  vyos:
    auth: vyos_auth
    walk:
      - 1.3.6.1.2.1.1.3    # sysUpTime
      - 1.3.6.1.2.1.2      # ifTable
      - 1.3.6.1.2.1.31.1.1 # ifXTable
    lookups:
      - source_indexes: [ifIndex]
        lookup: ifDescr
    overrides:
      ifDescr:
        type: DisplayString
